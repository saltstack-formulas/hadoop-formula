<?xml version="1.0" encoding="UTF-8"?>
<?xml-stylesheet type="text/xsl" href="configuration.xsl"?>

{% set namenode_host = salt['mine.get']('roles:hadoop_master', 'network.interfaces', 'grain').keys()|first() -%}

<configuration>
    <property>
        <name>io.native.lib.available</name>
        <value>true</value>
    </property>
     
    <property>
        <name>fs.default.name</name>
        <value>hdfs://{{ namenode_host }}:{{ pillar.get('namenode_port') }}</value>
    </property>
     
    <property>
        <name>hadoop.tmp.dir</name>
        <value>{{ pillar.get('hadoop_temp_directory') }}</value>
    </property>

{% if pillar.get('hadoopconf-core-site') %}
{% for name, dict in pillar.get('hadoopconf-core-site').items() %}
    <property>
        <name>{{ name }}</name>
        {% for key, value in dict.iteritems() %}<{{ key }}>{{ value }}</{{key}}>{% endfor %}
    </property>
{% endfor -%}
{% endif -%}

</configuration>
